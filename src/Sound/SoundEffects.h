#pragma once
#include "raylib.h"

// Audio effect: lowpass filter
void AudioProcessEffectLPF(void* buffer, unsigned int frames);   // Audio effect: lowpass filter
//static void AudioProcessEffectDelay(void* buffer, unsigned int frames);